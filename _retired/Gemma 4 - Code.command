#!/bin/bash
# Thin wrapper — the real launcher lives in the repo at
# $HOME/Desktop/Local AI Setup/launchers/. Edit that file, not this one.
# This wrapper exists so Finder can show a custom icon (symlinks can't).
exec "/Users/dtribe/Desktop/PROJECTS/Local AI Setup/launchers/Gemma 4 Code.command" "$@"
